!!!
%html(lang="en")
  %head
    %meta(charset="UTF-8")
      %title= @user ? @user + ' - OctoWatcher' : 'OctoWatcher'
      %meta(name="description" content="Your watched repositories of Github can be easily seen by list display. And README can be checked on that spot.")
      %meta(name="keywords"    content="Your watched repositories of Github can be easily seen by list display. And README can be checked on that spot.")
      %link(rel="stylesheet"   href="/bootstrap.css")
      %link(rel="stylesheet"   href="/application.css")
      %script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js")
      %script(src="/application.js")
      / Le HTML5 shim, for IE6-8 support of HTML elements
      / [if lt IE 9]
      /   <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
      / [endif]
      :javascript
        var _gaq = _gaq || [];
        _gaq.push(['_setAccount', 'UA-29890113-1']);
        _gaq.push(['_trackPageview']);

        (function() {
          var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
          ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
          var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
        })();
  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %a.brand(href="/") OctoWatcher
          .nav-collapse
            %ul.nav
              %li.divider-vertical
              %li
                %form.navbar-search.pull-left(action="/search")
                  %input.search-query.span2(type="text" name="user" value="#{@user}" placeholder="Search...")
            %ul.nav.pull-right
              %li
                %p#all-close ALL Close
    #wrap-container
      .container
        = yield

    %footer
      .container
        %p created_by <a href="https://twitter.com/#!/Daic_h">@Daic-h</a>. Source Code <a href="https://github.com/daic-h/octowatcher">daic-h/octowatcher</a>